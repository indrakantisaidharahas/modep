name: C++ Build

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Compile
      run: g++ -std=c++17 -fopenmp test.cpp -o test

    - name: Run
      run: ./test
